import { IStyledComponent } from 'styled-components';
import { BaseComponentProps } from '../types';
export declare type ToolbarProps = {
    show?: boolean;
} & BaseComponentProps;
declare const Toolbar: IStyledComponent<'web', ToolbarProps>;
export default Toolbar;

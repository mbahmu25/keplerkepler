import React from 'react';
import { RGBColor } from '@kepler.gl/types';
interface DatasetLabelType {
    dataset: {
        color: RGBColor;
        label: string;
    };
}
declare const DatasetLabel: ({ dataset }: DatasetLabelType) => React.JSX.Element;
export default DatasetLabel;

import { FC } from 'react';
declare type Props = {
    width: number;
    scaleType: string;
    domain: [number, number];
    fieldType: string;
    range: [number, number];
};
declare const RadiusLegend: FC<Props>;
export default RadiusLegend;

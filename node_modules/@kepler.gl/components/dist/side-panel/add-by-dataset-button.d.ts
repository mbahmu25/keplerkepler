import React from 'react';
import { Datasets } from '@kepler.gl/table';
export declare type AddByDatasetButtonProps = {
    datasets: Datasets;
    onAdd: (dataId: string) => void;
    width: string;
    buttonIntlId: string;
    inactive?: boolean;
    className?: string;
};
declare const AddByDatasetButton: React.FC<AddByDatasetButtonProps>;
export default AddByDatasetButton;
